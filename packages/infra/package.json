{
  "name": "soul-infra",
  "version": "0.0.0",
  "main": "index.ts",
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/gcp": "^7.0.0",
    "@pulumi/kubernetes": "^4.0.0",
    "@pulumi/random": "^4.0.0"
  },
  "scripts": {
    "build": "echo 'No build for infra'"
  }
}