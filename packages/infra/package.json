{
	"name": "@engram/infra",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"init": "tofu init",
		"validate": "tofu validate",
		"fmt": "tofu fmt -recursive",
		"plan": "tofu plan",
		"up": "tofu apply -auto-approve",
		"down": "tofu destroy -auto-approve",
		"output": "tofu output",
		"test": "tofu test",
		"test:unit": "tofu test -filter=tests/server.tftest.hcl -filter=tests/firewall.tftest.hcl -filter=tests/ssh.tftest.hcl",
		"refresh": "tofu refresh",
		"state": "tofu state list",
		"import:server": "tofu import hcloud_server.engram $(hcloud server describe engram -o json | jq -r .id)",
		"import:ssh": "tofu import hcloud_ssh_key.engram $(hcloud ssh-key describe engram-key -o json | jq -r .id)"
	}
}
