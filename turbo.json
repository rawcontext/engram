{
	"$schema": "https://turbo.build/schema.json",
	"remoteCache": {
		"signature": true
	},
	"globalDependencies": ["tsconfig.json", "biome.json"],
	"globalPassThroughEnv": ["CI", "NODE_ENV", "GITHUB_ACTIONS"],
	"globalEnv": ["REDIS_URL", "QDRANT_URL"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["$TURBO_DEFAULT$", "!**/*.test.ts", "!**/*.spec.ts"],
			"outputs": ["dist/**", ".next/**", "!.next/cache/**"],
			"outputLogs": "new-only"
		},
		"typecheck": {
			"dependsOn": ["^build"],
			"inputs": ["$TURBO_DEFAULT$", "tsconfig.json"],
			"outputLogs": "new-only"
		},
		"lint": {
			"inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/biome.json"],
			"outputLogs": "new-only"
		},
		"//#lint:run": {
			"inputs": ["apps/**/*.ts", "apps/**/*.tsx", "packages/**/*.ts", "biome.json"],
			"outputLogs": "new-only"
		},
		"//#format:run": {
			"inputs": ["apps/**/*.ts", "apps/**/*.tsx", "packages/**/*.ts", "biome.json"],
			"outputLogs": "new-only"
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"test": {
			"inputs": ["src/**/*.py", "tests/**/*.py", "pyproject.toml"],
			"outputs": ["coverage/**", ".coverage", "htmlcov/**"],
			"outputLogs": "new-only"
		}
	}
}
