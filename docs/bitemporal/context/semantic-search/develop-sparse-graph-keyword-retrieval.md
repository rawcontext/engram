# Bead: Develop Sparse Graph Keyword Retrieval

## Context
Execute the Sparse part (BM25/SPLADE).

## Goal
Wrapper around `qdrant.search({ vector_name: 'sparse' })`.

## Acceptance Criteria
-   [ ] `SparseRetriever` class.
